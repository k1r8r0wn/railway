<%= form_for(@station) do |f| %>
  <%= render 'common/errors', resource: @station %>

  <div class="field">
    <%= f.label :title, 'Title:' %><br>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
